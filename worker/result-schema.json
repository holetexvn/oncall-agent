{
  "type": "object",
  "properties": {
    "incident_id": { "type": "string" },
    "root_cause": { "type": "string", "description": "Một đoạn ngắn, tiếng Việt" },
    "confidence": { "type": "number", "minimum": 0, "maximum": 1 },
    "action": { "type": "string", "enum": ["pr_opened", "needs_human", "not_a_bug", "suspicious_input"] },
    "pr_url": { "type": ["string", "null"] },
    "files_changed": { "type": "array", "items": { "type": "string" } },
    "summary_for_human": { "type": "string", "description": "3 câu, gửi Telegram" }
  },
  "required": ["incident_id", "root_cause", "confidence", "action", "summary_for_human"]
}
